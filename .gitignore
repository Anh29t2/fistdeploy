# Bỏ qua thư viện nặng (Git không cần lưu cái này vì npm install sẽ tự cài lại)
node_modules/
*/node_modules/

# Bỏ qua file chứa mật khẩu (CỰC KỲ QUAN TRỌNG)
.env
*/.env

# Bỏ qua các file build (file rác khi chạy lệnh build)
dist/
build/
*/dist/
*/build/

# Bỏ qua file hệ thống
.DS_Store
Thumbs.db